<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.vetext">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VETEXT">
        <activity android:name=".Afterregestrationdocactivty"></activity>
        <activity android:name=".Extension" />
        <activity android:name=".VetDoctor" />
        <activity android:name=".Farmerdetails" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AfterRegistration"
            android:theme="@style/Theme.Design.NoActionBar" />
        <activity android:name=".Register" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>